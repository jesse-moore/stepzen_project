schema
    @sdl(
        files: [
            "airtable/event.graphql"
            "airtable/race.graphql"
            "airtable/event-races.graphql"
            "airtable/marker.graphql"
            "airtable/race-markers.graphql"
        ]
    ) {
    query: Query
}
