schema
    @sdl(
        files: [
            "airtable/event.graphql"
            "airtable/race.graphql"
            "airtable/event-races.graphql"
            "airtable/aidStation.graphql"
            "airtable/race-aidStations.graphql"
        ]
    ) {
    query: Query
}
